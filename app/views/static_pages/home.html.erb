<% provide(:title, 'Home') %>

<h1>Home</h1>
<p>
  This is the home page for the
  <a href="http://railstutorial.org/">Ruby on Rails Tutorial</a>
  sample application.
</p>
<p>A note (The distinction between the two types of embedded Ruby is that
  &lt;% ... %&gt;
  executes the code inside, while
  &lt;%= ... %&gt;
  executes it and inserts the result into the template.)
</p>

